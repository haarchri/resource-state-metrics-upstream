# `jsonnet/`

This directory contains [`jsonnet`](https://jsonnet.org/) libraries for deploying resource-state-metrics. Use the relevant targets in the `Makefile` to lint and re-generate the manifests.
